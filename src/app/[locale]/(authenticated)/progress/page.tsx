import PlaceholderPage from '@/components/PlaceholderPage';

/**
 * Analyse training performance over time.
 * Covers exercise-specific progression such as Bench Press weight and reps,
 * personal records, training frequency, and workout consistency.
 * Later this page will include volume, statistics, and progress charts.
 */
export default function ProgressPage() {
  return <PlaceholderPage titleKey="progress" />;
}
