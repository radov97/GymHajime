import PlaceholderPage from '@/components/PlaceholderPage';

/**
 * The user's Monday–Sunday weekly workout routine.
 * Users assign a workout or training plan to each day and can create, edit,
 * or remove workouts. Workouts contain exercises selected from the Exercise
 * Library with planned sets and reps. Rest days can also be configured.
 */
export default function SchedulePage() {
  return <PlaceholderPage titleKey="schedule" />;
}
