import PlaceholderPage from '@/components/PlaceholderPage';

/**
 * Manage the user's GymHajime account and preferences.
 * Includes username, display name, profile picture, bio, privacy controls,
 * Discover/search visibility, public weekly-schedule visibility, and other
 * account-related settings.
 */
export default function SettingsPage() {
  return <PlaceholderPage titleKey="settings" />;
}
