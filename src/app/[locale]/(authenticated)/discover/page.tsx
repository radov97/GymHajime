import PlaceholderPage from '@/components/PlaceholderPage';

/**
 * Search for GymHajime users and display searchable public profiles.
 * Visitors can view public weekly schedules and the exercises and sets within
 * those workouts. Later, users can copy a public workout into their schedule.
 */
export default function DiscoverPage() {
  return <PlaceholderPage titleKey="discover" />;
}
