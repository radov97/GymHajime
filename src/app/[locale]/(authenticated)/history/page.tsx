import PlaceholderPage from '@/components/PlaceholderPage';

/**
 * Record of training sessions the user actually completed.
 * Schedule represents the plan; History represents what happened.
 * Shows session date, workout, exercises, sets, reps, and weights performed,
 * with the ability to open an old session and inspect the completed details.
 */
export default function HistoryPage() {
  return <PlaceholderPage titleKey="history" />;
}
